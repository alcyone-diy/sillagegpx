<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect the root to the public folder
    RewriteRule ^$ public/ [L]
    
    # Redirect everything else to the public folder
    RewriteRule (.*) public/$1 [L]
</IfModule>
